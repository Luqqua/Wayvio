Public GitHub source release prepared on 2026-06-02.

Changes made for this export:

- excluded runtime dependencies and local runtime artifacts
- removed real environment files and local database artifacts
- anonymized example legal/contact configuration
- removed hardcoded third-party mail credentials from example config
- aligned repository metadata to AGPL-3.0-or-later
- replaced large theme preview images with a shared placeholder
- removed generated `public/css` and `public/js` build artifacts
- removed sample PNG marketing images and local/user media artifacts

The original application logic remains in the source tree, but this archive is intended as a sanitized source distribution for public hosting.

This public source release represents the published Wayvio application code. It does not include the full private operating environment, unpublished internal APIs, or infrastructure used for the hosted service at `wayvio.de`.
